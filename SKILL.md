---
name: find-skills-cn
description: >-
  快速找到最匹配的 Agent Skill。理解中文或英文需求，提取关键词去搜索可安装的
  Agent Skill，输出少量高匹配结果：名称、介绍、下载量（installs）或
  GitHub star 数（均只作参考，排序以匹配度为准），每条附可点击链接。
  只做搜索和输出结果，不做安装。不要推荐 MCP server、Plugin、App、
  Agent、软件包或普通开源项目当作 Skill。
---

# Find Skills CN

快速、全面地为用户找到最匹配的可安装 Agent Skill。

## 理解需求（中文适配）

1. 先理解用户要**做什么**，用一句话复述确认方向，不要照着字面词机械搜索。
2. 从需求里提取搜索关键词：核心词（技术/任务）+ 扩展词（相关领域、同义词、中英文变体）。扩展词只在能提高命中率时加，一般不超过 2~3 个。
3. 判断需求明确度：
   - **明确**（具体技术/任务，如 "Python 数据分析"、"做 PPT"）→ 输出 **3 个** 最匹配的结果，第 1 个是主推荐，其余两个是备选。
   - **模糊/宽泛**（如 "有没有好用的 skill"、"找论文相关的"）→ 输出 **最多 5 个** 覆盖不同方向的候选。
   - 找不到 3 个可信结果时，有几个给几个，不硬凑。
4. 不要问一堆澄清问题。需求太模糊时先按最可能的理解搜，用结果数量体现覆盖度。

## 搜索

按 `references/search.md` 串行搜索，**每个来源限时 15 秒**，超时跳过不重试：

1. **skills.sh**（主）→ 结果 installs 仅作参考，排序以匹配度为准
2. **GitHub**（兜底）→ 结果 star 仅作参考，排序以匹配度为准
3. **官方渠道**（Anthropic `anthropics/skills`，仅作技能全览，无法关键词搜索；官方技能已被 skills.sh 收录）→ 见 `references/search.md`

搜索只取元数据：名称、描述、来源、install 数 / star 数、链接。找到足够结果就停。不读任何 agent 的本地已安装目录。

## 推荐输出

每条推荐带四块信息：**名称 + 链接**、**详细介绍**、**元数据**、**链接**。用横向分隔线（`---`）把候选块隔开，块与块之间留空行；只有 1 个候选时省略分隔线。模板如下，如实填写、缺项留空：

```
**1. 技能名**
> 介绍：写够 3~5 句。把下面的点说清楚，宁详勿略：
> - **功能**：它具体解决什么问题（如文生视频 / 图生视频 / 数字人）
> - **怎么用**：装完后怎么触发、流程大致什么样
> - **场景**：适合谁、什么场景下用它
> - **依赖**：要不要 API Key、平台账号、本地运行时或额外组件

- 元数据：**installs 和 star 都写**，两者互相印证（skills.sh 来源给 `installs`，GitHub 来源给 `⭐`；查得到就都列）。缺项留空，不编造。两者反差悬殊时（如 installs 很高但 star 几乎为 0），如实标注「⭐ 很低，热度存疑」，不替它找补
- 链接：[github.com/<owner>/<repo>](https://github.com/<owner>/<repo>)（skills.sh 来源按 `source` 字段构造；GitHub 来源直接用 `html_url`）

---

**2. 技能名**
> 介绍：3~5 句，同样覆盖功能 / 怎么用 / 场景 / 依赖。

- 元数据：……
- 链接：[……](……)
```

输出开头用一句话点名主推荐，例如「**主推荐**：第 1 个」。**排序以匹配度为准**——最贴合需求的排最前；installs / star 只在匹配度相近时作参考打破平局，不唯热度。只输出高匹配的，无关结果不展示。找不到可信结果就直接说明没有找到，不拿 MCP、Plugin 或普通项目凑数。

## 边界

- 本 skill **只做搜索和输出结果**：不安装、不更新、不执行候选脚本、不做安全检查、不读候选 SKILL.md。
- 输出的是可安装的 Agent Skill 推荐与链接，是否安装、怎么装由用户自己决定。
- 不把"搜索结果里的热度高"说成"适合你"。
